{
  "name": "conflict-tracker",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "seed": "pnpm --filter @conflict-tracker/web seed",
    "ingest": "pnpm --filter @conflict-tracker/web ingest",
    "ingest:backfill": "pnpm --filter @conflict-tracker/web exec tsx ../../scripts/backfill-iran-week.ts",
    "ingest:reparse": "pnpm --filter @conflict-tracker/web exec tsx ../../scripts/reparse-existing-events.ts",
    "local:up": "./scripts/local-up.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "overrides": {
      "loglayer": "^9.1.0"
    }
  }
}
