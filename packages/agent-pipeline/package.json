{
  "name": "@conflict-tracker/agent-pipeline",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'lint skipped for agent-pipeline'"
  },
  "dependencies": {
    "@accelint/geo": "latest",
    "@conflict-tracker/data-model": "workspace:*",
    "@conflict-tracker/plugin-registry": "workspace:*",
    "openai": "^4.75.0"
  }
}
