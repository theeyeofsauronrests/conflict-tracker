{
  "name": "@conflict-tracker/ingest-plugins",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'lint skipped for ingest-plugins'"
  },
  "dependencies": {
    "@conflict-tracker/data-model": "workspace:*",
    "@conflict-tracker/plugin-registry": "workspace:*",
    "fast-xml-parser": "^4.5.1"
  }
}
