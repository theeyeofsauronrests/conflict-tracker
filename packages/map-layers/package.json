{
  "name": "@conflict-tracker/map-layers",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'lint skipped for map-layers'"
  },
  "dependencies": {
    "@deck.gl/core": "9.2.10",
    "@deck.gl/layers": "9.2.10",
    "@deck.gl/aggregation-layers": "9.2.10",
    "@conflict-tracker/data-model": "workspace:*"
  }
}
